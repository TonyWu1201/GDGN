protocols: [eval-lco, eval-ldo-kt, eval-ldo-so, eval-db]
models: [gdgn]
folds: [0, 1, 2, 3, 4]
seeds: [42, 3407, 8128]
base_overrides:
  micro_batch: 4
  cell_bypass_mode: residual
  learnable_alpha: true
  dual_cell: true
variants:
  a00: {pretrain_ckpt: null, use_main_drug_emb: false}
  a01: {pretrain_ckpt: null, use_main_drug_emb: true}
  a10: {pretrain_ckpt: "data/model/pretrain/strict/{split_id}/fold-{fold:02d}/seed-{seed:04d}/best_encoder.pt", use_main_drug_emb: false}
  a11: {pretrain_ckpt: "data/model/pretrain/strict/{split_id}/fold-{fold:02d}/seed-{seed:04d}/best_encoder.pt", use_main_drug_emb: true}
